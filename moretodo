Compiler remaining work (live tracker)
======================================

Last updated: 2026-03-06

Done
----

[x] 1. Compile non-class targets
- Current IR status is fully specialized for both primary ontologies:
  - `/tmp/compiled-shacl-223p/srcgen.ir.json`: `specialization_ready=true`, unsupported node/property shapes = `0/0`.
  - `/tmp/compiled-shacl-brick/srcgen.ir.json`: `specialization_ready=true`, unsupported node/property shapes = `0/0`.

[x] 2. Compile complex property paths
- Current lowering/codegen supports sequence/alternative/inverse/repetition path forms.
- Current 223 + Brick IR snapshots show no unsupported property shapes.

[x] 3. Stabilize srcgen code chunking for large generated modules
- Item-based chunk splitting is in place so include chunks stay syntax-balanced.
- 223 generated binary now builds and runs validation (no truncated/unclosed chunk failures).

[x] 4. Compiled-manifest dependency-resolution overhead reduction
- `lib/tests/compiled_manifest_test.rs` now reuses a shared `Cargo.lock` across generated workspaces.
- Verified in strict-srcgen shard run: first workspace resolves/locks; subsequent workspaces reuse lock and avoid repeated locking.

Open priorities
---------------

[x] A. Remove runtime conformance fallback dependency for logical shape checks
- Full-AOT execution now hard-disables runtime shape-conformance fallback in generated code paths.
- Strict hard-fail compiled-manifest run is green:
  - `SHFTY_COMPILED_MANIFEST_COMPILER=srcgen`
  - `SHFTY_COMPILED_MANIFEST_STRICT_FULL_AOT=1`
  - `SHFTY_COMPILED_MANIFEST_STRICT_INCOMPLETE_IS_ERROR=1`
  - Result: `337 passed; 0 failed`.

[x] B. Strict full-AOT inference behavior is gated correctly
- Generated inference runtime now errors in strict full-AOT mode if unsupported inference residue remains instead of silently skipping.
- This provides deterministic strict-mode behavior and prevents hidden runtime fallback during inference.

[x] C. Full strict compiled-manifest parity/report gate plumbing
- Hard-fail toggle is wired and exercised:
  - `SHFTY_COMPILED_MANIFEST_STRICT_INCOMPLETE_IS_ERROR=1`.
- Strict srcgen compiled-manifest parity run completes without incomplete skips (see item A result).

[x] D. Perf gates instrumented end-to-end (compile + latency + memory)
- `srcgen_strict_full_aot_perf_gate_223_and_brick` now tracks and prints:
  - runtime CLI release build/compile durations
  - generated 223/Brick compile durations
  - runtime vs compiled median validation latency per dataset
  - runtime vs compiled median peak RSS (KiB) when `/usr/bin/time` is available
- Added reuse knobs to avoid repeated huge compiles:
  - `SHFTY_PERF_GATE_BIN_223`
  - `SHFTY_PERF_GATE_BIN_BRICK`
- Added faster release compile setting in gate path:
  - `CARGO_PROFILE_RELEASE_DEBUG=0`

Current status
--------------

- No blocking AOT parity items remain in this tracker.
