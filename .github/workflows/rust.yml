name: Rust

concurrency:
  group: rust-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [ "main", "shacl-formalism" ]
  pull_request:
    branches: [ "main", "shacl-formalism" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5

    - name: Install Rust toolchain
      uses: dtolnay/rust-toolchain@stable
      with:
        components: rustfmt, clippy

    - name: Cargo fmt
      run: cargo fmt --all -- --check

    - name: Cargo clippy
      run: cargo clippy --workspace --all-targets --all-features -- -D warnings

    - name: Cargo test
      run: cargo test --workspace

    # TEMPORARILY DISABLED: Compiled tests are failing and need investigation
    # - name: Cargo test (compiled manifest suite)
    #   run: cargo test -p shifty-shacl --features compiled-tests --test compiled_manifest_test
